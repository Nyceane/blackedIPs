NODE_ENV = development

# API url
REACT_APP_API_URL=

# Stripe public key
REACT_APP_STRIPE_PUBLIC_KEY = 

# number of users per page in users table
REACT_APP_USERS_PER_PAGE=30


USER_STORAGE_KEY = TIM_STORAGE_KEY